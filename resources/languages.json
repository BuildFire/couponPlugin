{
  "sections": {
    "general": {
      "title": "General",
      "labels": {
        "clear": {
          "title": "Clear",
          "maxLength": 6,
          "required": true,
          "defaultValue": "Clear"
        },
        "noExpirationDate": {
          "title": "No expiration date",
          "maxLength": 30,
          "required": true,
          "defaultValue": "No expiration date"
        },
        "expires": {
          "title": "Expires",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Expires"
        },
        "redeemed": {
          "title": "Redeemed",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Redeemed"
        },
        "failedToLoadGoogleMapsApi": {
          "title": "Failed to load Google Maps API.",
          "maxLength": 80,
          "required": true,
          "defaultValue": "Failed to load Google Maps API."
        },
        "itemNoLongerExists": {
          "title": "This item no longer exists!",
          "maxLength": 80,
          "required": true,
          "defaultValue": "This item no longer exists!"
        },
        "at": {
          "title": "At",
          "maxLength": 8,
          "required": true,
          "defaultValue": "at"
        },
        "away": {
          "title": "Away",
          "maxLength": 8,
          "required": true,
          "defaultValue": "away"
        }
      }
    },
    "navigation": {
      "title": "Navigation",
      "labels": {
        "list": {
          "title": "List",
          "maxLength": 15,
          "required": true,
          "defaultValue": "List"
        },
        "map": {
          "title": "Map",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Map"
        },
        "category": {
          "title": "Category",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Category"
        },
        "saved": {
          "title": "Saved",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Saved"
        },
        "filterApplied": {
          "title": "Filter Applied Label",
          "maxLength": 20,
          "required": true,
          "defaultValue": "Filter Applied"
        }
      }
    },
    "itemDetails": {
      "title": "Item Details Screen",
      "labels": {
        "openMoreOptions": {
          "title": "Open More Options Button",
          "maxLength": 30,
          "required": true,
          "defaultValue": "Open More Options"
        },
         "redeemThisItem": {
          "title": "Redeem this item label",
          "maxLength": 30,
          "required": true,
          "defaultValue": "Redeem this Item"
        },
        "itemRedeemedTimeago": {
          "title": "Item redeemed label",
          "maxLength": 30,
          "required": true,
          "defaultValue": "Item redeemed"
        }
      }
    },
    "listScreen": {
      "title": "List Screen",
      "labels": {
      }
    },
    "mapScreen": {
      "title": "Map Screen",
      "labels": {
        "expAbbreviation": {
          "title": "Expiration abbreviation",
          "maxLength": 8,
          "required": true,
          "defaultValue": "Exp."
        },
        "close": {
          "title": "Close label",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Close"
        }
      }
    },
    "savedScreen": {
      "title": "Saved Items Screen",
      "labels": {
        "searchItemNameOrSummary": {
          "title": "Search items",
          "maxLength": 50,
          "required": true,
          "defaultValue": "Search item name or summary..."
        },
        "noCouponsSaved": {
          "title": "No coupons saved.",
          "maxLength": 30,
          "required": true,
          "defaultValue": "No coupons saved."
        }
      }
    },
    "categories": {
      "title": "Categories Filter",
      "labels": {
        "enterKeywordsOrPhrase": {
          "title": "Search filter",
          "maxLength": 40,
          "required": true,
          "defaultValue": "Enter keywords or phrase"
        },
        "distance": {
          "title": "Distance Range Label",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Distance"
        },
        "sortingOptions": {
          "title": "Sorting Options",
          "maxLength": 20,
          "required": true,
          "defaultValue": "Sorting Options"
        },
        "default": {
          "title": "Default",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Default"
        },
        "closestToMe": {
          "title": "Closest to Me",
          "maxLength": 20,
          "required": true,
          "defaultValue": "Closest to Me"
        },
        "categories": {
          "title": "Categories",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Categories"
        },
        "allItems": {
          "title": "All Items Label",
          "maxLength": 20,
          "required": true,
          "defaultValue": "All Items"
        },
        "cancel": {
          "title": "Cancel",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Cancel"
        },
        "apply": {
          "title": "Apply",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Apply"
        },
        "reset": {
          "title": "Reset",
          "maxLength": 15,
          "required": true,
          "defaultValue": "Reset"
        },
        "enableLocationSharing": {
          "title": "Enable Location sharing to activate Closest to Me.",
          "maxLength": 60,
          "required": true,
          "defaultValue": "Enable Location sharing to activate Closest to Me."
        }
      }
    },
    "confirmations": {
      "title": "Confirmation Messages",
      "labels": {
        "itemSavedConfirmation": {
          "title": "Item Saved!",
          "maxLength": 20,
          "required": true,
          "defaultValue": "Item Saved!"
        },
        "itemRedeemedConfirmation": {
          "title": "Item Redeemed",
          "maxLength": 20,
          "required": true,
          "defaultValue": "Item Redeemed"
        },
        "itemRemovedFromSaved": {
          "title": "Item removed from saved list",
          "maxLength": 30,
          "required": true,
          "defaultValue": "Item removed from saved list"
        }
      }
    }
  }
}
